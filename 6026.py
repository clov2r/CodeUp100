"""
6026::실수 2개 입력받아 합 계산

"""
a =input()
b=input()
c = float(a) + float(b)
print(c)

"""
c = float(a) + float(b) 해당 코드를 c=float(a+b) 이렇게도 가능한가? > X
두 문자열 a와 b를 이어붙인 후에 실수로 변환하는데,
이는 두 문자열을 연결하는 것이기 때문에 불가능
"""
